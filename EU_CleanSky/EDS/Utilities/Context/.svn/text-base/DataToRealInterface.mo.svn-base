within EU_CleanSky.EDS.Utilities.Context;
partial block DataToRealInterface
  "Block computes a real outputs from a shared context stored in a record."
  extends .Modelica.Blocks.Interfaces.BlockIcon;

  parameter Integer n;
  .Modelica.Blocks.Interfaces.RealOutput y[n] annotation (Placement(
        transformation(extent={{80,-20},{120,20}}), iconTransformation(extent={
            {100,-10},{120,10}})));
  DataInput dataInput annotation (Placement(transformation(extent={{-96,0},{-76,
            20}}), iconTransformation(extent={{-116,-20},{-76,20}})));
equation
  //y[1] = dataInpput.xxx;
  annotation (
    preferredView="text",
    Icon(coordinateSystem(preserveAspectRatio=false, extent={{-100,-100},{100,
            100}}), graphics),
    Diagram(coordinateSystem(preserveAspectRatio=false, extent={{-100,-100},{
            100,100}}), graphics));
end DataToRealInterface;
