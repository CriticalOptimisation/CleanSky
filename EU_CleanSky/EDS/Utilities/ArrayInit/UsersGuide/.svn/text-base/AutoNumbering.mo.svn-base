within EU_CleanSky.EDS.Utilities.ArrayInit.UsersGuide;
model AutoNumbering "Automatic Numbering"

  annotation (Documentation(info="<html>
<p>The normal use of a data source guarantees that when the constructor is called with the same data source and the same index, it always produces the same set of parameter values. This example violates this rule using a special data source which disregards the position index and returns consecutive increasing integers starting with 1. The component parameters are therefore numbered. </p>
<p>See example<a href=\"Modelica://EU_CleanSky.EDS.Utilities.ArrayInit.Examples.AutomaticNumbering\"> AutomaticNumbering</a>.</p>
<p><img src=\"modelica://EU_CleanSky/EDS/Utilities/Resources/AutomaticNumbering.png\"/></p>
</html>"));
end AutoNumbering;
