within EU_CleanSky.EDS.Utilities.HierarchicalConnection.Base;
partial model PartialInputOutputProxy
  "A partial proxy with an Input icon. Add a single connector to the right side to create an input, or to the left side to create and output."
  extends PartialProxy;
  extends Icons.InputOutputProxy;
  annotation (defaultComponentName="input1", Diagram(coordinateSystem(extent={{
            -100,-100},{100,100}}, preserveAspectRatio=false), graphics));
end PartialInputOutputProxy;
