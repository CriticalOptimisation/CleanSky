within EU_CleanSky.EDS.Utilities.Context;
partial block RealToDataInterface
  "Block computes a Data record from real inputs."
  extends .Modelica.Blocks.Interfaces.BlockIcon;

  parameter Integer n=0;
  .Modelica.Blocks.Interfaces.RealInput u[n] annotation (Placement(
        transformation(extent={{-140,-20},{-100,20}}), iconTransformation(
          extent={{-140,-20},{-100,20}})));
  DataOutput dataOutput annotation (Placement(transformation(extent={{100,-10},
            {120,10}}), iconTransformation(extent={{100,-10},{120,10}})));
equation
  //dataOutput.xxx = u[1];
  annotation (preferredView="text");
end RealToDataInterface;
